/**
 * Habit Atlas style reminder: the home route is a tactile, local-first fieldbook—not a generic SaaS dashboard.
 */
import AppShell from "@/components/AppShell";

export default function Home() {
  return <AppShell />;
}
